# Telegram Monitor System

Гибридная система мониторинга корпоративных Telegram-чатов.

## Архитектура

Система построена на принципе **разделения ответственности**:

- **Collector** (Telethon) — сбор данных из чатов
- **Analyzer** (LLM + Whisper) — анализ контента
- **Manager** (aiogram) — уведомления и управление
- **Storage** (SQLite + Google Sheets) — хранение данных

## Структура проекта

```
admin_chat_bot/
├── config/             # Конфигурация
│   ├── .env.example    # Шаблон переменных окружения
│   └── settings.py     # Pydantic настройки (TODO)
├── data/
│   ├── sessions/       # Telethon session files
│   └── temp/           # Временные файлы (голосовые)
├── logs/               # Логи приложения
├── src/
│   ├── collector/      # Telethon Userbot
│   ├── manager/        # aiogram Bot
│   ├── core/           # Бизнес-логика (Analyzer, LLM, Whisper)
│   ├── storage/        # SQLite + Google Sheets
│   ├── scheduler/      # APScheduler задачи
│   ├── models/         # Pydantic модели
│   └── utils/          # Утилиты, логирование
├── tests/              # Тесты
├── main.py             # Точка входа
└── requirements.txt    # Зависимости
```

## Установка

```bash
# 1. Создание виртуального окружения
python -m venv venv
venv\Scripts\activate  # Windows

# 2. Установка зависимостей
pip install -r requirements.txt

# 3. Настройка конфигурации
copy config\.env.example config\.env
# Заполните .env своими данными
```

## Документация

- [Техническое задание](TZ_hybrid.md)
- [План реализации](implementation_hybrid.md)

## Статус разработки

🚧 **В разработке** — Этап 1 (MVP)
