# Руководство по работе с виртуальным окружением

**Дата создания**: 2026-02-02  
**Расположение**: `d:\Чат-боты\Задания Николая\Бот Админ\admin_chat_bot\venv`

---

## ✅ Что установлено

Виртуальное окружение Python **полностью размещено на диске D:\** в папке проекта.

### Структура папки venv:

```
venv/
├── Scripts/          # Скрипты активации и исполняемые файлы
│   ├── activate      # Для bash/zsh
│   ├── Activate.ps1  # Для PowerShell (Windows)
│   ├── python.exe    # Копия Python для этого проекта
│   └── pip.exe       # Менеджер пакетов
├── Lib/              # Все установленные библиотеки (на диске D:\)
│   └── site-packages/
│       ├── telethon/
│       ├── aiogram/
│       ├── gspread_asyncio/
│       └── ... (все остальные библиотеки)
└── pyvenv.cfg        # Конфигурация виртуального окружения
```

---

## 📦 Установленные библиотеки

Все библиотеки из `requirements.txt` установлены на диск **D:\**:

| Библиотека | Версия | Назначение |
|------------|--------|------------|
| **Telethon** | 1.42.0 | Userbot для сбора данных |
| **aiogram** | 3.24.0 | Официальный бот для уведомлений |
| **APScheduler** | 3.11.2 | Планировщик задач |
| **aiohttp** | 3.13.3 | HTTP-клиент для CometAPI |
| **gspread_asyncio** | 2.0.0 | Работа с Google Sheets |
| **google-auth** | 2.48.0 | Авторизация Google Cloud |
| **pydantic** | 2.12.5 | Валидация данных |
| **pydantic-settings** | 2.12.0 | Управление конфигурацией |
| **aiosqlite** | 0.22.1 | Асинхронная работа с SQLite |
| **loguru** | 0.7.3 | Продвинутое логирование |
| **python-dotenv** | 1.2.1 | Загрузка .env файлов |

**+ 31 дополнительная зависимость** (автоматически установленная)

---

## 🚀 Как использовать виртуальное окружение

### 1. Активация (перед каждым запуском проекта)

**Windows PowerShell** (рекомендуется):
```powershell
cd "d:\Чат-боты\Задания Николая\Бот Админ\admin_chat_bot"
.\venv\Scripts\Activate.ps1
```

**Windows CMD**:
```cmd
cd "d:\Чат-боты\Задания Николая\Бот Админ\admin_chat_bot"
.\venv\Scripts\activate.bat
```

**Признак успешной активации**: В начале строки появится `(venv)`

```powershell
(venv) PS D:\Чат-боты\Задания Николая\Бот Админ\admin_chat_bot>
```

---

### 2. Запуск проекта (после активации)

```powershell
# Убедитесь, что venv активировано (есть префикс (venv))
python main.py
```

---

### 3. Деактивация (после завершения работы)

```powershell
deactivate
```

Префикс `(venv)` исчезнет.

---

## 🔧 Управление зависимостями

### Установка новой библиотеки

```powershell
# 1. Активируйте venv
.\venv\Scripts\Activate.ps1

# 2. Установите библиотеку
pip install название_библиотеки

# 3. Обновите requirements.txt
pip freeze > requirements.txt
```

### Удаление библиотеки

```powershell
# 1. Активируйте venv
.\venv\Scripts\Activate.ps1

# 2. Удалите библиотеку
pip uninstall название_библиотеки

# 3. Обновите requirements.txt
pip freeze > requirements.txt
```

### Проверка установленных библиотек

```powershell
# Активируйте venv
.\venv\Scripts\Activate.ps1

# Посмотрите список
pip list
```

---

## ⚠️ Важные замечания

### 1. Всегда активируйте venv перед работой

❌ **Неправильно** (работа без активации):
```powershell
PS D:\> python main.py  # Использует системный Python с диска C:\
```

✅ **Правильно** (с активацией):
```powershell
PS D:\> cd "d:\Чат-боты\Задания Николая\Бот Админ\admin_chat_bot"
PS D:\> .\venv\Scripts\Activate.ps1
(venv) PS D:\> python main.py  # Использует Python из venv на диске D:\
```

---

### 2. Проверка расположения Python

```powershell
# Активируйте venv
.\venv\Scripts\Activate.ps1

# Проверьте путь к Python
(venv) PS > (Get-Command python).Source
# Должно показать: D:\Чат-боты\Задания Николая\Бот Админ\admin_chat_bot\venv\Scripts\python.exe
```

---

### 3. Папка venv НЕ должна попасть в Git

Файл `.gitignore` должен содержать:
```gitignore
# Виртуальное окружение
venv/
*.pyc
__pycache__/
```

---

## 🔄 Пересоздание виртуального окружения

Если нужно полностью пересоздать venv:

```powershell
# 1. Удалите старую папку venv
Remove-Item -Recurse -Force .\venv

# 2. Создайте новое venv
python -m venv venv

# 3. Активируйте
.\venv\Scripts\Activate.ps1

# 4. Установите зависимости
pip install -r requirements.txt
```

---

## 📊 Размер виртуального окружения

Текущий размер папки `venv`: **~200-300 МБ**

**Включает**:
- Копию Python 3.11
- Все библиотеки из requirements.txt
- Их зависимости (транзитивные)

---

## 🆘 Troubleshooting

### Проблема: "Невозможно выполнить скрипт Activate.ps1"

**Причина**: Политика выполнения PowerShell блокирует скрипты

**Решение**:
```powershell
# Разрешить выполнение скриптов (один раз)
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

---

### Проблема: "pip не найден"

**Причина**: venv не активирован

**Решение**:
```powershell
.\venv\Scripts\Activate.ps1
```

---

## ✅ Преимущества этого подхода

1. ✅ **Все на диске D:\** — ничего не устанавливается на диск C:\
2. ✅ **Изоляция** — библиотеки этого проекта не влияют на другие проекты
3. ✅ **Портативность** — можно скопировать папку проекта вместе с venv
4. ✅ **Контроль версий** — точно знаете, какие версии библиотек используются
5. ✅ **Легко удалить** — просто удалите папку venv

---

## 📝 Следующие шаги

1. Создать файл конфигурации `.env`
2. Настроить подключение к Telegram API
3. Настроить Google Sheets API
4. Запустить проект: `python main.py`
